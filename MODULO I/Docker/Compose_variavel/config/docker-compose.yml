version: '3.3'

services:
  db:
    image: 'mysql:5.7'
    volumes:
      - /path/to/db_data/var/lib/mysql
    restart: always
    env_file:
      - C:\Abelardo\Estagio Compass\Data & Analytcs\fast_pb_da\MODULO I\Docker\Compose_variavel\config\db.env
  wordpress:
    depends_on:
      - db
    image: wordpress:latest
    ports:
      - "8000:80"
    restart: always
    environment:
      WORDPRESS_DB_HOST: db:3306
      WORDPRESS_DB_USER: abelardo
      WORDPRESS_DB_PASSWORD: secret
      WORDPRESS_DB_NAME: wordpress
volumes:
  db_data: {}